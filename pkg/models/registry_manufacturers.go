// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// RegistryManufacturer is an object representing the database table.
type RegistryManufacturer struct {
	ID         uint      `boil:"id" json:"id" toml:"id" yaml:"id"`
	RegistryID uint      `boil:"registry_id" json:"registry_id" toml:"registry_id" yaml:"registry_id"`
	Title      string    `boil:"title" json:"title" toml:"title" yaml:"title"`
	Country    string    `boil:"country" json:"country" toml:"country" yaml:"country"`
	CreatedAt  null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt  null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *registryManufacturerR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L registryManufacturerL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RegistryManufacturerColumns = struct {
	ID         string
	RegistryID string
	Title      string
	Country    string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	RegistryID: "registry_id",
	Title:      "title",
	Country:    "country",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

// RegistryManufacturerRels is where relationship names are stored.
var RegistryManufacturerRels = struct {
	Registry string
}{
	Registry: "Registry",
}

// registryManufacturerR is where relationships are stored.
type registryManufacturerR struct {
	Registry *Registry
}

// NewStruct creates a new relationship struct
func (*registryManufacturerR) NewStruct() *registryManufacturerR {
	return &registryManufacturerR{}
}

// registryManufacturerL is where Load methods for each relationship are stored.
type registryManufacturerL struct{}

var (
	registryManufacturerColumns               = []string{"id", "registry_id", "title", "country", "created_at", "updated_at"}
	registryManufacturerColumnsWithoutDefault = []string{"registry_id", "title", "country", "created_at", "updated_at"}
	registryManufacturerColumnsWithDefault    = []string{"id"}
	registryManufacturerPrimaryKeyColumns     = []string{"id"}
)

type (
	// RegistryManufacturerSlice is an alias for a slice of pointers to RegistryManufacturer.
	// This should generally be used opposed to []RegistryManufacturer.
	RegistryManufacturerSlice []*RegistryManufacturer
	// RegistryManufacturerHook is the signature for custom RegistryManufacturer hook methods
	RegistryManufacturerHook func(context.Context, boil.ContextExecutor, *RegistryManufacturer) error

	registryManufacturerQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	registryManufacturerType                 = reflect.TypeOf(&RegistryManufacturer{})
	registryManufacturerMapping              = queries.MakeStructMapping(registryManufacturerType)
	registryManufacturerPrimaryKeyMapping, _ = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, registryManufacturerPrimaryKeyColumns)
	registryManufacturerInsertCacheMut       sync.RWMutex
	registryManufacturerInsertCache          = make(map[string]insertCache)
	registryManufacturerUpdateCacheMut       sync.RWMutex
	registryManufacturerUpdateCache          = make(map[string]updateCache)
	registryManufacturerUpsertCacheMut       sync.RWMutex
	registryManufacturerUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var registryManufacturerBeforeInsertHooks []RegistryManufacturerHook
var registryManufacturerBeforeUpdateHooks []RegistryManufacturerHook
var registryManufacturerBeforeDeleteHooks []RegistryManufacturerHook
var registryManufacturerBeforeUpsertHooks []RegistryManufacturerHook

var registryManufacturerAfterInsertHooks []RegistryManufacturerHook
var registryManufacturerAfterSelectHooks []RegistryManufacturerHook
var registryManufacturerAfterUpdateHooks []RegistryManufacturerHook
var registryManufacturerAfterDeleteHooks []RegistryManufacturerHook
var registryManufacturerAfterUpsertHooks []RegistryManufacturerHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RegistryManufacturer) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RegistryManufacturer) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RegistryManufacturer) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RegistryManufacturer) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RegistryManufacturer) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RegistryManufacturer) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RegistryManufacturer) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RegistryManufacturer) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RegistryManufacturer) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryManufacturerAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRegistryManufacturerHook registers your hook function for all future operations.
func AddRegistryManufacturerHook(hookPoint boil.HookPoint, registryManufacturerHook RegistryManufacturerHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		registryManufacturerBeforeInsertHooks = append(registryManufacturerBeforeInsertHooks, registryManufacturerHook)
	case boil.BeforeUpdateHook:
		registryManufacturerBeforeUpdateHooks = append(registryManufacturerBeforeUpdateHooks, registryManufacturerHook)
	case boil.BeforeDeleteHook:
		registryManufacturerBeforeDeleteHooks = append(registryManufacturerBeforeDeleteHooks, registryManufacturerHook)
	case boil.BeforeUpsertHook:
		registryManufacturerBeforeUpsertHooks = append(registryManufacturerBeforeUpsertHooks, registryManufacturerHook)
	case boil.AfterInsertHook:
		registryManufacturerAfterInsertHooks = append(registryManufacturerAfterInsertHooks, registryManufacturerHook)
	case boil.AfterSelectHook:
		registryManufacturerAfterSelectHooks = append(registryManufacturerAfterSelectHooks, registryManufacturerHook)
	case boil.AfterUpdateHook:
		registryManufacturerAfterUpdateHooks = append(registryManufacturerAfterUpdateHooks, registryManufacturerHook)
	case boil.AfterDeleteHook:
		registryManufacturerAfterDeleteHooks = append(registryManufacturerAfterDeleteHooks, registryManufacturerHook)
	case boil.AfterUpsertHook:
		registryManufacturerAfterUpsertHooks = append(registryManufacturerAfterUpsertHooks, registryManufacturerHook)
	}
}

// One returns a single registryManufacturer record from the query.
func (q registryManufacturerQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RegistryManufacturer, error) {
	o := &RegistryManufacturer{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for registry_manufacturers")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RegistryManufacturer records from the query.
func (q registryManufacturerQuery) All(ctx context.Context, exec boil.ContextExecutor) (RegistryManufacturerSlice, error) {
	var o []*RegistryManufacturer

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RegistryManufacturer slice")
	}

	if len(registryManufacturerAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RegistryManufacturer records in the query.
func (q registryManufacturerQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count registry_manufacturers rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q registryManufacturerQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if registry_manufacturers exists")
	}

	return count > 0, nil
}

// Registry pointed to by the foreign key.
func (o *RegistryManufacturer) Registry(mods ...qm.QueryMod) registryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.RegistryID),
	}

	queryMods = append(queryMods, mods...)

	query := Registries(queryMods...)
	queries.SetFrom(query.Query, "`registries`")

	return query
}

// LoadRegistry allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (registryManufacturerL) LoadRegistry(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRegistryManufacturer interface{}, mods queries.Applicator) error {
	var slice []*RegistryManufacturer
	var object *RegistryManufacturer

	if singular {
		object = maybeRegistryManufacturer.(*RegistryManufacturer)
	} else {
		slice = *maybeRegistryManufacturer.(*[]*RegistryManufacturer)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &registryManufacturerR{}
		}
		args = append(args, object.RegistryID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &registryManufacturerR{}
			}

			for _, a := range args {
				if a == obj.RegistryID {
					continue Outer
				}
			}

			args = append(args, obj.RegistryID)
		}
	}

	query := NewQuery(qm.From(`registries`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Registry")
	}

	var resultSlice []*Registry
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Registry")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for registries")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for registries")
	}

	if len(registryManufacturerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Registry = foreign
		if foreign.R == nil {
			foreign.R = &registryR{}
		}
		foreign.R.RegistryManufacturers = append(foreign.R.RegistryManufacturers, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RegistryID == foreign.ID {
				local.R.Registry = foreign
				if foreign.R == nil {
					foreign.R = &registryR{}
				}
				foreign.R.RegistryManufacturers = append(foreign.R.RegistryManufacturers, local)
				break
			}
		}
	}

	return nil
}

// SetRegistry of the registryManufacturer to the related item.
// Sets o.R.Registry to related.
// Adds o to related.R.RegistryManufacturers.
func (o *RegistryManufacturer) SetRegistry(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Registry) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `registry_manufacturers` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"registry_id"}),
		strmangle.WhereClause("`", "`", 0, registryManufacturerPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RegistryID = related.ID
	if o.R == nil {
		o.R = &registryManufacturerR{
			Registry: related,
		}
	} else {
		o.R.Registry = related
	}

	if related.R == nil {
		related.R = &registryR{
			RegistryManufacturers: RegistryManufacturerSlice{o},
		}
	} else {
		related.R.RegistryManufacturers = append(related.R.RegistryManufacturers, o)
	}

	return nil
}

// RegistryManufacturers retrieves all the records using an executor.
func RegistryManufacturers(mods ...qm.QueryMod) registryManufacturerQuery {
	mods = append(mods, qm.From("`registry_manufacturers`"))
	return registryManufacturerQuery{NewQuery(mods...)}
}

// FindRegistryManufacturer retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRegistryManufacturer(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*RegistryManufacturer, error) {
	registryManufacturerObj := &RegistryManufacturer{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `registry_manufacturers` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, registryManufacturerObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from registry_manufacturers")
	}

	return registryManufacturerObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RegistryManufacturer) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_manufacturers provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if queries.MustTime(o.CreatedAt).IsZero() {
		queries.SetScanner(&o.CreatedAt, currTime)
	}
	if queries.MustTime(o.UpdatedAt).IsZero() {
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryManufacturerColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	registryManufacturerInsertCacheMut.RLock()
	cache, cached := registryManufacturerInsertCache[key]
	registryManufacturerInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			registryManufacturerColumns,
			registryManufacturerColumnsWithDefault,
			registryManufacturerColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `registry_manufacturers` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `registry_manufacturers` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `registry_manufacturers` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, registryManufacturerPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into registry_manufacturers")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryManufacturerMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_manufacturers")
	}

CacheNoHooks:
	if !cached {
		registryManufacturerInsertCacheMut.Lock()
		registryManufacturerInsertCache[key] = cache
		registryManufacturerInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RegistryManufacturer.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RegistryManufacturer) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	queries.SetScanner(&o.UpdatedAt, currTime)

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	registryManufacturerUpdateCacheMut.RLock()
	cache, cached := registryManufacturerUpdateCache[key]
	registryManufacturerUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			registryManufacturerColumns,
			registryManufacturerPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update registry_manufacturers, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `registry_manufacturers` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, registryManufacturerPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, append(wl, registryManufacturerPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update registry_manufacturers row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for registry_manufacturers")
	}

	if !cached {
		registryManufacturerUpdateCacheMut.Lock()
		registryManufacturerUpdateCache[key] = cache
		registryManufacturerUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q registryManufacturerQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for registry_manufacturers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for registry_manufacturers")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RegistryManufacturerSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryManufacturerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `registry_manufacturers` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryManufacturerPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in registryManufacturer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all registryManufacturer")
	}
	return rowsAff, nil
}

var mySQLRegistryManufacturerUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RegistryManufacturer) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_manufacturers provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if queries.MustTime(o.CreatedAt).IsZero() {
		queries.SetScanner(&o.CreatedAt, currTime)
	}
	queries.SetScanner(&o.UpdatedAt, currTime)

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryManufacturerColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRegistryManufacturerUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	registryManufacturerUpsertCacheMut.RLock()
	cache, cached := registryManufacturerUpsertCache[key]
	registryManufacturerUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			registryManufacturerColumns,
			registryManufacturerColumnsWithDefault,
			registryManufacturerColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			registryManufacturerColumns,
			registryManufacturerPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert registry_manufacturers, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "registry_manufacturers", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `registry_manufacturers` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for registry_manufacturers")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryManufacturerMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(registryManufacturerType, registryManufacturerMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for registry_manufacturers")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_manufacturers")
	}

CacheNoHooks:
	if !cached {
		registryManufacturerUpsertCacheMut.Lock()
		registryManufacturerUpsertCache[key] = cache
		registryManufacturerUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RegistryManufacturer record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RegistryManufacturer) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryManufacturer provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), registryManufacturerPrimaryKeyMapping)
	sql := "DELETE FROM `registry_manufacturers` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from registry_manufacturers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for registry_manufacturers")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q registryManufacturerQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no registryManufacturerQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registry_manufacturers")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_manufacturers")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RegistryManufacturerSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryManufacturer slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(registryManufacturerBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryManufacturerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `registry_manufacturers` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryManufacturerPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registryManufacturer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_manufacturers")
	}

	if len(registryManufacturerAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RegistryManufacturer) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRegistryManufacturer(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RegistryManufacturerSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RegistryManufacturerSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryManufacturerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `registry_manufacturers`.* FROM `registry_manufacturers` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryManufacturerPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RegistryManufacturerSlice")
	}

	*o = slice

	return nil
}

// RegistryManufacturerExists checks if the RegistryManufacturer row exists.
func RegistryManufacturerExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `registry_manufacturers` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if registry_manufacturers exists")
	}

	return exists, nil
}
