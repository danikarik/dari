// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// RegistryFieldStat is an object representing the database table.
type RegistryFieldStat struct {
	ID                uint        `boil:"id" json:"id" toml:"id" yaml:"id"`
	RegistryID        null.Uint   `boil:"registry_id" json:"registry_id,omitempty" toml:"registry_id" yaml:"registry_id,omitempty"`
	RegistryJournalID uint        `boil:"registry_journal_id" json:"registry_journal_id" toml:"registry_journal_id" yaml:"registry_journal_id"`
	Label             string      `boil:"label" json:"label" toml:"label" yaml:"label"`
	OldValue          null.String `boil:"old_value" json:"old_value,omitempty" toml:"old_value" yaml:"old_value,omitempty"`
	NewValue          null.String `boil:"new_value" json:"new_value,omitempty" toml:"new_value" yaml:"new_value,omitempty"`
	ErrorMSG          null.String `boil:"error_msg" json:"error_msg,omitempty" toml:"error_msg" yaml:"error_msg,omitempty"`
	CreatedAt         null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt         null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *registryFieldStatR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L registryFieldStatL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RegistryFieldStatColumns = struct {
	ID                string
	RegistryID        string
	RegistryJournalID string
	Label             string
	OldValue          string
	NewValue          string
	ErrorMSG          string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "id",
	RegistryID:        "registry_id",
	RegistryJournalID: "registry_journal_id",
	Label:             "label",
	OldValue:          "old_value",
	NewValue:          "new_value",
	ErrorMSG:          "error_msg",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
}

// RegistryFieldStatRels is where relationship names are stored.
var RegistryFieldStatRels = struct {
	Registry        string
	RegistryJournal string
}{
	Registry:        "Registry",
	RegistryJournal: "RegistryJournal",
}

// registryFieldStatR is where relationships are stored.
type registryFieldStatR struct {
	Registry        *Registry
	RegistryJournal *RegistryJournal
}

// NewStruct creates a new relationship struct
func (*registryFieldStatR) NewStruct() *registryFieldStatR {
	return &registryFieldStatR{}
}

// registryFieldStatL is where Load methods for each relationship are stored.
type registryFieldStatL struct{}

var (
	registryFieldStatColumns               = []string{"id", "registry_id", "registry_journal_id", "label", "old_value", "new_value", "error_msg", "created_at", "updated_at"}
	registryFieldStatColumnsWithoutDefault = []string{"registry_id", "registry_journal_id", "label", "old_value", "new_value", "error_msg", "created_at", "updated_at"}
	registryFieldStatColumnsWithDefault    = []string{"id"}
	registryFieldStatPrimaryKeyColumns     = []string{"id"}
)

type (
	// RegistryFieldStatSlice is an alias for a slice of pointers to RegistryFieldStat.
	// This should generally be used opposed to []RegistryFieldStat.
	RegistryFieldStatSlice []*RegistryFieldStat
	// RegistryFieldStatHook is the signature for custom RegistryFieldStat hook methods
	RegistryFieldStatHook func(context.Context, boil.ContextExecutor, *RegistryFieldStat) error

	registryFieldStatQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	registryFieldStatType                 = reflect.TypeOf(&RegistryFieldStat{})
	registryFieldStatMapping              = queries.MakeStructMapping(registryFieldStatType)
	registryFieldStatPrimaryKeyMapping, _ = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, registryFieldStatPrimaryKeyColumns)
	registryFieldStatInsertCacheMut       sync.RWMutex
	registryFieldStatInsertCache          = make(map[string]insertCache)
	registryFieldStatUpdateCacheMut       sync.RWMutex
	registryFieldStatUpdateCache          = make(map[string]updateCache)
	registryFieldStatUpsertCacheMut       sync.RWMutex
	registryFieldStatUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var registryFieldStatBeforeInsertHooks []RegistryFieldStatHook
var registryFieldStatBeforeUpdateHooks []RegistryFieldStatHook
var registryFieldStatBeforeDeleteHooks []RegistryFieldStatHook
var registryFieldStatBeforeUpsertHooks []RegistryFieldStatHook

var registryFieldStatAfterInsertHooks []RegistryFieldStatHook
var registryFieldStatAfterSelectHooks []RegistryFieldStatHook
var registryFieldStatAfterUpdateHooks []RegistryFieldStatHook
var registryFieldStatAfterDeleteHooks []RegistryFieldStatHook
var registryFieldStatAfterUpsertHooks []RegistryFieldStatHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RegistryFieldStat) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RegistryFieldStat) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RegistryFieldStat) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RegistryFieldStat) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RegistryFieldStat) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RegistryFieldStat) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RegistryFieldStat) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RegistryFieldStat) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RegistryFieldStat) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryFieldStatAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRegistryFieldStatHook registers your hook function for all future operations.
func AddRegistryFieldStatHook(hookPoint boil.HookPoint, registryFieldStatHook RegistryFieldStatHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		registryFieldStatBeforeInsertHooks = append(registryFieldStatBeforeInsertHooks, registryFieldStatHook)
	case boil.BeforeUpdateHook:
		registryFieldStatBeforeUpdateHooks = append(registryFieldStatBeforeUpdateHooks, registryFieldStatHook)
	case boil.BeforeDeleteHook:
		registryFieldStatBeforeDeleteHooks = append(registryFieldStatBeforeDeleteHooks, registryFieldStatHook)
	case boil.BeforeUpsertHook:
		registryFieldStatBeforeUpsertHooks = append(registryFieldStatBeforeUpsertHooks, registryFieldStatHook)
	case boil.AfterInsertHook:
		registryFieldStatAfterInsertHooks = append(registryFieldStatAfterInsertHooks, registryFieldStatHook)
	case boil.AfterSelectHook:
		registryFieldStatAfterSelectHooks = append(registryFieldStatAfterSelectHooks, registryFieldStatHook)
	case boil.AfterUpdateHook:
		registryFieldStatAfterUpdateHooks = append(registryFieldStatAfterUpdateHooks, registryFieldStatHook)
	case boil.AfterDeleteHook:
		registryFieldStatAfterDeleteHooks = append(registryFieldStatAfterDeleteHooks, registryFieldStatHook)
	case boil.AfterUpsertHook:
		registryFieldStatAfterUpsertHooks = append(registryFieldStatAfterUpsertHooks, registryFieldStatHook)
	}
}

// One returns a single registryFieldStat record from the query.
func (q registryFieldStatQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RegistryFieldStat, error) {
	o := &RegistryFieldStat{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for registry_field_stats")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RegistryFieldStat records from the query.
func (q registryFieldStatQuery) All(ctx context.Context, exec boil.ContextExecutor) (RegistryFieldStatSlice, error) {
	var o []*RegistryFieldStat

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RegistryFieldStat slice")
	}

	if len(registryFieldStatAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RegistryFieldStat records in the query.
func (q registryFieldStatQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count registry_field_stats rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q registryFieldStatQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if registry_field_stats exists")
	}

	return count > 0, nil
}

// Registry pointed to by the foreign key.
func (o *RegistryFieldStat) Registry(mods ...qm.QueryMod) registryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.RegistryID),
	}

	queryMods = append(queryMods, mods...)

	query := Registries(queryMods...)
	queries.SetFrom(query.Query, "`registries`")

	return query
}

// RegistryJournal pointed to by the foreign key.
func (o *RegistryFieldStat) RegistryJournal(mods ...qm.QueryMod) registryJournalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.RegistryJournalID),
	}

	queryMods = append(queryMods, mods...)

	query := RegistryJournals(queryMods...)
	queries.SetFrom(query.Query, "`registry_journals`")

	return query
}

// LoadRegistry allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (registryFieldStatL) LoadRegistry(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRegistryFieldStat interface{}, mods queries.Applicator) error {
	var slice []*RegistryFieldStat
	var object *RegistryFieldStat

	if singular {
		object = maybeRegistryFieldStat.(*RegistryFieldStat)
	} else {
		slice = *maybeRegistryFieldStat.(*[]*RegistryFieldStat)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &registryFieldStatR{}
		}
		args = append(args, object.RegistryID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &registryFieldStatR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.RegistryID) {
					continue Outer
				}
			}

			args = append(args, obj.RegistryID)
		}
	}

	query := NewQuery(qm.From(`registries`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Registry")
	}

	var resultSlice []*Registry
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Registry")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for registries")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for registries")
	}

	if len(registryFieldStatAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Registry = foreign
		if foreign.R == nil {
			foreign.R = &registryR{}
		}
		foreign.R.RegistryFieldStats = append(foreign.R.RegistryFieldStats, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RegistryID, foreign.ID) {
				local.R.Registry = foreign
				if foreign.R == nil {
					foreign.R = &registryR{}
				}
				foreign.R.RegistryFieldStats = append(foreign.R.RegistryFieldStats, local)
				break
			}
		}
	}

	return nil
}

// LoadRegistryJournal allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (registryFieldStatL) LoadRegistryJournal(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRegistryFieldStat interface{}, mods queries.Applicator) error {
	var slice []*RegistryFieldStat
	var object *RegistryFieldStat

	if singular {
		object = maybeRegistryFieldStat.(*RegistryFieldStat)
	} else {
		slice = *maybeRegistryFieldStat.(*[]*RegistryFieldStat)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &registryFieldStatR{}
		}
		args = append(args, object.RegistryJournalID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &registryFieldStatR{}
			}

			for _, a := range args {
				if a == obj.RegistryJournalID {
					continue Outer
				}
			}

			args = append(args, obj.RegistryJournalID)
		}
	}

	query := NewQuery(qm.From(`registry_journals`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RegistryJournal")
	}

	var resultSlice []*RegistryJournal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RegistryJournal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for registry_journals")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for registry_journals")
	}

	if len(registryFieldStatAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.RegistryJournal = foreign
		if foreign.R == nil {
			foreign.R = &registryJournalR{}
		}
		foreign.R.RegistryFieldStats = append(foreign.R.RegistryFieldStats, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RegistryJournalID == foreign.ID {
				local.R.RegistryJournal = foreign
				if foreign.R == nil {
					foreign.R = &registryJournalR{}
				}
				foreign.R.RegistryFieldStats = append(foreign.R.RegistryFieldStats, local)
				break
			}
		}
	}

	return nil
}

// SetRegistry of the registryFieldStat to the related item.
// Sets o.R.Registry to related.
// Adds o to related.R.RegistryFieldStats.
func (o *RegistryFieldStat) SetRegistry(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Registry) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `registry_field_stats` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"registry_id"}),
		strmangle.WhereClause("`", "`", 0, registryFieldStatPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RegistryID, related.ID)
	if o.R == nil {
		o.R = &registryFieldStatR{
			Registry: related,
		}
	} else {
		o.R.Registry = related
	}

	if related.R == nil {
		related.R = &registryR{
			RegistryFieldStats: RegistryFieldStatSlice{o},
		}
	} else {
		related.R.RegistryFieldStats = append(related.R.RegistryFieldStats, o)
	}

	return nil
}

// RemoveRegistry relationship.
// Sets o.R.Registry to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *RegistryFieldStat) RemoveRegistry(ctx context.Context, exec boil.ContextExecutor, related *Registry) error {
	var err error

	queries.SetScanner(&o.RegistryID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("registry_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.Registry = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RegistryFieldStats {
		if queries.Equal(o.RegistryID, ri.RegistryID) {
			continue
		}

		ln := len(related.R.RegistryFieldStats)
		if ln > 1 && i < ln-1 {
			related.R.RegistryFieldStats[i] = related.R.RegistryFieldStats[ln-1]
		}
		related.R.RegistryFieldStats = related.R.RegistryFieldStats[:ln-1]
		break
	}
	return nil
}

// SetRegistryJournal of the registryFieldStat to the related item.
// Sets o.R.RegistryJournal to related.
// Adds o to related.R.RegistryFieldStats.
func (o *RegistryFieldStat) SetRegistryJournal(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RegistryJournal) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `registry_field_stats` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"registry_journal_id"}),
		strmangle.WhereClause("`", "`", 0, registryFieldStatPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RegistryJournalID = related.ID
	if o.R == nil {
		o.R = &registryFieldStatR{
			RegistryJournal: related,
		}
	} else {
		o.R.RegistryJournal = related
	}

	if related.R == nil {
		related.R = &registryJournalR{
			RegistryFieldStats: RegistryFieldStatSlice{o},
		}
	} else {
		related.R.RegistryFieldStats = append(related.R.RegistryFieldStats, o)
	}

	return nil
}

// RegistryFieldStats retrieves all the records using an executor.
func RegistryFieldStats(mods ...qm.QueryMod) registryFieldStatQuery {
	mods = append(mods, qm.From("`registry_field_stats`"))
	return registryFieldStatQuery{NewQuery(mods...)}
}

// FindRegistryFieldStat retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRegistryFieldStat(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*RegistryFieldStat, error) {
	registryFieldStatObj := &RegistryFieldStat{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `registry_field_stats` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, registryFieldStatObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from registry_field_stats")
	}

	return registryFieldStatObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RegistryFieldStat) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_field_stats provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if queries.MustTime(o.CreatedAt).IsZero() {
		queries.SetScanner(&o.CreatedAt, currTime)
	}
	if queries.MustTime(o.UpdatedAt).IsZero() {
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryFieldStatColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	registryFieldStatInsertCacheMut.RLock()
	cache, cached := registryFieldStatInsertCache[key]
	registryFieldStatInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			registryFieldStatColumns,
			registryFieldStatColumnsWithDefault,
			registryFieldStatColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `registry_field_stats` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `registry_field_stats` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `registry_field_stats` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, registryFieldStatPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into registry_field_stats")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryFieldStatMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_field_stats")
	}

CacheNoHooks:
	if !cached {
		registryFieldStatInsertCacheMut.Lock()
		registryFieldStatInsertCache[key] = cache
		registryFieldStatInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RegistryFieldStat.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RegistryFieldStat) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	queries.SetScanner(&o.UpdatedAt, currTime)

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	registryFieldStatUpdateCacheMut.RLock()
	cache, cached := registryFieldStatUpdateCache[key]
	registryFieldStatUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			registryFieldStatColumns,
			registryFieldStatPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update registry_field_stats, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `registry_field_stats` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, registryFieldStatPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, append(wl, registryFieldStatPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update registry_field_stats row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for registry_field_stats")
	}

	if !cached {
		registryFieldStatUpdateCacheMut.Lock()
		registryFieldStatUpdateCache[key] = cache
		registryFieldStatUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q registryFieldStatQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for registry_field_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for registry_field_stats")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RegistryFieldStatSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryFieldStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `registry_field_stats` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryFieldStatPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in registryFieldStat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all registryFieldStat")
	}
	return rowsAff, nil
}

var mySQLRegistryFieldStatUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RegistryFieldStat) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_field_stats provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if queries.MustTime(o.CreatedAt).IsZero() {
		queries.SetScanner(&o.CreatedAt, currTime)
	}
	queries.SetScanner(&o.UpdatedAt, currTime)

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryFieldStatColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRegistryFieldStatUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	registryFieldStatUpsertCacheMut.RLock()
	cache, cached := registryFieldStatUpsertCache[key]
	registryFieldStatUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			registryFieldStatColumns,
			registryFieldStatColumnsWithDefault,
			registryFieldStatColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			registryFieldStatColumns,
			registryFieldStatPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert registry_field_stats, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "registry_field_stats", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `registry_field_stats` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for registry_field_stats")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryFieldStatMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(registryFieldStatType, registryFieldStatMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for registry_field_stats")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_field_stats")
	}

CacheNoHooks:
	if !cached {
		registryFieldStatUpsertCacheMut.Lock()
		registryFieldStatUpsertCache[key] = cache
		registryFieldStatUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RegistryFieldStat record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RegistryFieldStat) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryFieldStat provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), registryFieldStatPrimaryKeyMapping)
	sql := "DELETE FROM `registry_field_stats` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from registry_field_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for registry_field_stats")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q registryFieldStatQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no registryFieldStatQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registry_field_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_field_stats")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RegistryFieldStatSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryFieldStat slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(registryFieldStatBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryFieldStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `registry_field_stats` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryFieldStatPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registryFieldStat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_field_stats")
	}

	if len(registryFieldStatAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RegistryFieldStat) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRegistryFieldStat(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RegistryFieldStatSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RegistryFieldStatSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryFieldStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `registry_field_stats`.* FROM `registry_field_stats` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryFieldStatPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RegistryFieldStatSlice")
	}

	*o = slice

	return nil
}

// RegistryFieldStatExists checks if the RegistryFieldStat row exists.
func RegistryFieldStatExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `registry_field_stats` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if registry_field_stats exists")
	}

	return exists, nil
}
