// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// RegistryRecommendation is an object representing the database table.
type RegistryRecommendation struct {
	ID         uint `boil:"id" json:"id" toml:"id" yaml:"id"`
	RegistryID uint `boil:"registry_id" json:"registry_id" toml:"registry_id" yaml:"registry_id"`
	ProductID  uint `boil:"product_id" json:"product_id" toml:"product_id" yaml:"product_id"`
	IsApplied  bool `boil:"is_applied" json:"is_applied" toml:"is_applied" yaml:"is_applied"`

	R *registryRecommendationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L registryRecommendationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RegistryRecommendationColumns = struct {
	ID         string
	RegistryID string
	ProductID  string
	IsApplied  string
}{
	ID:         "id",
	RegistryID: "registry_id",
	ProductID:  "product_id",
	IsApplied:  "is_applied",
}

// RegistryRecommendationRels is where relationship names are stored.
var RegistryRecommendationRels = struct {
	Product  string
	Registry string
}{
	Product:  "Product",
	Registry: "Registry",
}

// registryRecommendationR is where relationships are stored.
type registryRecommendationR struct {
	Product  *Product
	Registry *Registry
}

// NewStruct creates a new relationship struct
func (*registryRecommendationR) NewStruct() *registryRecommendationR {
	return &registryRecommendationR{}
}

// registryRecommendationL is where Load methods for each relationship are stored.
type registryRecommendationL struct{}

var (
	registryRecommendationColumns               = []string{"id", "registry_id", "product_id", "is_applied"}
	registryRecommendationColumnsWithoutDefault = []string{"registry_id", "product_id"}
	registryRecommendationColumnsWithDefault    = []string{"id", "is_applied"}
	registryRecommendationPrimaryKeyColumns     = []string{"id"}
)

type (
	// RegistryRecommendationSlice is an alias for a slice of pointers to RegistryRecommendation.
	// This should generally be used opposed to []RegistryRecommendation.
	RegistryRecommendationSlice []*RegistryRecommendation
	// RegistryRecommendationHook is the signature for custom RegistryRecommendation hook methods
	RegistryRecommendationHook func(context.Context, boil.ContextExecutor, *RegistryRecommendation) error

	registryRecommendationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	registryRecommendationType                 = reflect.TypeOf(&RegistryRecommendation{})
	registryRecommendationMapping              = queries.MakeStructMapping(registryRecommendationType)
	registryRecommendationPrimaryKeyMapping, _ = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, registryRecommendationPrimaryKeyColumns)
	registryRecommendationInsertCacheMut       sync.RWMutex
	registryRecommendationInsertCache          = make(map[string]insertCache)
	registryRecommendationUpdateCacheMut       sync.RWMutex
	registryRecommendationUpdateCache          = make(map[string]updateCache)
	registryRecommendationUpsertCacheMut       sync.RWMutex
	registryRecommendationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
)

var registryRecommendationBeforeInsertHooks []RegistryRecommendationHook
var registryRecommendationBeforeUpdateHooks []RegistryRecommendationHook
var registryRecommendationBeforeDeleteHooks []RegistryRecommendationHook
var registryRecommendationBeforeUpsertHooks []RegistryRecommendationHook

var registryRecommendationAfterInsertHooks []RegistryRecommendationHook
var registryRecommendationAfterSelectHooks []RegistryRecommendationHook
var registryRecommendationAfterUpdateHooks []RegistryRecommendationHook
var registryRecommendationAfterDeleteHooks []RegistryRecommendationHook
var registryRecommendationAfterUpsertHooks []RegistryRecommendationHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RegistryRecommendation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RegistryRecommendation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RegistryRecommendation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RegistryRecommendation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RegistryRecommendation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RegistryRecommendation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RegistryRecommendation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RegistryRecommendation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RegistryRecommendation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	for _, hook := range registryRecommendationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRegistryRecommendationHook registers your hook function for all future operations.
func AddRegistryRecommendationHook(hookPoint boil.HookPoint, registryRecommendationHook RegistryRecommendationHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		registryRecommendationBeforeInsertHooks = append(registryRecommendationBeforeInsertHooks, registryRecommendationHook)
	case boil.BeforeUpdateHook:
		registryRecommendationBeforeUpdateHooks = append(registryRecommendationBeforeUpdateHooks, registryRecommendationHook)
	case boil.BeforeDeleteHook:
		registryRecommendationBeforeDeleteHooks = append(registryRecommendationBeforeDeleteHooks, registryRecommendationHook)
	case boil.BeforeUpsertHook:
		registryRecommendationBeforeUpsertHooks = append(registryRecommendationBeforeUpsertHooks, registryRecommendationHook)
	case boil.AfterInsertHook:
		registryRecommendationAfterInsertHooks = append(registryRecommendationAfterInsertHooks, registryRecommendationHook)
	case boil.AfterSelectHook:
		registryRecommendationAfterSelectHooks = append(registryRecommendationAfterSelectHooks, registryRecommendationHook)
	case boil.AfterUpdateHook:
		registryRecommendationAfterUpdateHooks = append(registryRecommendationAfterUpdateHooks, registryRecommendationHook)
	case boil.AfterDeleteHook:
		registryRecommendationAfterDeleteHooks = append(registryRecommendationAfterDeleteHooks, registryRecommendationHook)
	case boil.AfterUpsertHook:
		registryRecommendationAfterUpsertHooks = append(registryRecommendationAfterUpsertHooks, registryRecommendationHook)
	}
}

// One returns a single registryRecommendation record from the query.
func (q registryRecommendationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RegistryRecommendation, error) {
	o := &RegistryRecommendation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for registry_recommendations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RegistryRecommendation records from the query.
func (q registryRecommendationQuery) All(ctx context.Context, exec boil.ContextExecutor) (RegistryRecommendationSlice, error) {
	var o []*RegistryRecommendation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RegistryRecommendation slice")
	}

	if len(registryRecommendationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RegistryRecommendation records in the query.
func (q registryRecommendationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count registry_recommendations rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q registryRecommendationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if registry_recommendations exists")
	}

	return count > 0, nil
}

// Product pointed to by the foreign key.
func (o *RegistryRecommendation) Product(mods ...qm.QueryMod) productQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.ProductID),
	}

	queryMods = append(queryMods, mods...)

	query := Products(queryMods...)
	queries.SetFrom(query.Query, "`products`")

	return query
}

// Registry pointed to by the foreign key.
func (o *RegistryRecommendation) Registry(mods ...qm.QueryMod) registryQuery {
	queryMods := []qm.QueryMod{
		qm.Where("id=?", o.RegistryID),
	}

	queryMods = append(queryMods, mods...)

	query := Registries(queryMods...)
	queries.SetFrom(query.Query, "`registries`")

	return query
}

// LoadProduct allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (registryRecommendationL) LoadProduct(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRegistryRecommendation interface{}, mods queries.Applicator) error {
	var slice []*RegistryRecommendation
	var object *RegistryRecommendation

	if singular {
		object = maybeRegistryRecommendation.(*RegistryRecommendation)
	} else {
		slice = *maybeRegistryRecommendation.(*[]*RegistryRecommendation)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &registryRecommendationR{}
		}
		args = append(args, object.ProductID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &registryRecommendationR{}
			}

			for _, a := range args {
				if a == obj.ProductID {
					continue Outer
				}
			}

			args = append(args, obj.ProductID)
		}
	}

	query := NewQuery(qm.From(`products`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Product")
	}

	var resultSlice []*Product
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Product")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for products")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for products")
	}

	if len(registryRecommendationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Product = foreign
		if foreign.R == nil {
			foreign.R = &productR{}
		}
		foreign.R.RegistryRecommendations = append(foreign.R.RegistryRecommendations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProductID == foreign.ID {
				local.R.Product = foreign
				if foreign.R == nil {
					foreign.R = &productR{}
				}
				foreign.R.RegistryRecommendations = append(foreign.R.RegistryRecommendations, local)
				break
			}
		}
	}

	return nil
}

// LoadRegistry allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (registryRecommendationL) LoadRegistry(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRegistryRecommendation interface{}, mods queries.Applicator) error {
	var slice []*RegistryRecommendation
	var object *RegistryRecommendation

	if singular {
		object = maybeRegistryRecommendation.(*RegistryRecommendation)
	} else {
		slice = *maybeRegistryRecommendation.(*[]*RegistryRecommendation)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &registryRecommendationR{}
		}
		args = append(args, object.RegistryID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &registryRecommendationR{}
			}

			for _, a := range args {
				if a == obj.RegistryID {
					continue Outer
				}
			}

			args = append(args, obj.RegistryID)
		}
	}

	query := NewQuery(qm.From(`registries`), qm.WhereIn(`id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Registry")
	}

	var resultSlice []*Registry
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Registry")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for registries")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for registries")
	}

	if len(registryRecommendationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Registry = foreign
		if foreign.R == nil {
			foreign.R = &registryR{}
		}
		foreign.R.RegistryRecommendations = append(foreign.R.RegistryRecommendations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.RegistryID == foreign.ID {
				local.R.Registry = foreign
				if foreign.R == nil {
					foreign.R = &registryR{}
				}
				foreign.R.RegistryRecommendations = append(foreign.R.RegistryRecommendations, local)
				break
			}
		}
	}

	return nil
}

// SetProduct of the registryRecommendation to the related item.
// Sets o.R.Product to related.
// Adds o to related.R.RegistryRecommendations.
func (o *RegistryRecommendation) SetProduct(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Product) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `registry_recommendations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"product_id"}),
		strmangle.WhereClause("`", "`", 0, registryRecommendationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProductID = related.ID
	if o.R == nil {
		o.R = &registryRecommendationR{
			Product: related,
		}
	} else {
		o.R.Product = related
	}

	if related.R == nil {
		related.R = &productR{
			RegistryRecommendations: RegistryRecommendationSlice{o},
		}
	} else {
		related.R.RegistryRecommendations = append(related.R.RegistryRecommendations, o)
	}

	return nil
}

// SetRegistry of the registryRecommendation to the related item.
// Sets o.R.Registry to related.
// Adds o to related.R.RegistryRecommendations.
func (o *RegistryRecommendation) SetRegistry(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Registry) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `registry_recommendations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"registry_id"}),
		strmangle.WhereClause("`", "`", 0, registryRecommendationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.RegistryID = related.ID
	if o.R == nil {
		o.R = &registryRecommendationR{
			Registry: related,
		}
	} else {
		o.R.Registry = related
	}

	if related.R == nil {
		related.R = &registryR{
			RegistryRecommendations: RegistryRecommendationSlice{o},
		}
	} else {
		related.R.RegistryRecommendations = append(related.R.RegistryRecommendations, o)
	}

	return nil
}

// RegistryRecommendations retrieves all the records using an executor.
func RegistryRecommendations(mods ...qm.QueryMod) registryRecommendationQuery {
	mods = append(mods, qm.From("`registry_recommendations`"))
	return registryRecommendationQuery{NewQuery(mods...)}
}

// FindRegistryRecommendation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRegistryRecommendation(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*RegistryRecommendation, error) {
	registryRecommendationObj := &RegistryRecommendation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `registry_recommendations` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, registryRecommendationObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from registry_recommendations")
	}

	return registryRecommendationObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RegistryRecommendation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_recommendations provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryRecommendationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	registryRecommendationInsertCacheMut.RLock()
	cache, cached := registryRecommendationInsertCache[key]
	registryRecommendationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			registryRecommendationColumns,
			registryRecommendationColumnsWithDefault,
			registryRecommendationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `registry_recommendations` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `registry_recommendations` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `registry_recommendations` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, registryRecommendationPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into registry_recommendations")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryRecommendationMapping["ID"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, identifierCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_recommendations")
	}

CacheNoHooks:
	if !cached {
		registryRecommendationInsertCacheMut.Lock()
		registryRecommendationInsertCache[key] = cache
		registryRecommendationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RegistryRecommendation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RegistryRecommendation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	registryRecommendationUpdateCacheMut.RLock()
	cache, cached := registryRecommendationUpdateCache[key]
	registryRecommendationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			registryRecommendationColumns,
			registryRecommendationPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update registry_recommendations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `registry_recommendations` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, registryRecommendationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, append(wl, registryRecommendationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update registry_recommendations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for registry_recommendations")
	}

	if !cached {
		registryRecommendationUpdateCacheMut.Lock()
		registryRecommendationUpdateCache[key] = cache
		registryRecommendationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q registryRecommendationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for registry_recommendations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for registry_recommendations")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RegistryRecommendationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryRecommendationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `registry_recommendations` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryRecommendationPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in registryRecommendation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all registryRecommendation")
	}
	return rowsAff, nil
}

var mySQLRegistryRecommendationUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RegistryRecommendation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no registry_recommendations provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(registryRecommendationColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLRegistryRecommendationUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	registryRecommendationUpsertCacheMut.RLock()
	cache, cached := registryRecommendationUpsertCache[key]
	registryRecommendationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			registryRecommendationColumns,
			registryRecommendationColumnsWithDefault,
			registryRecommendationColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			registryRecommendationColumns,
			registryRecommendationPrimaryKeyColumns,
		)

		if len(update) == 0 {
			return errors.New("models: unable to upsert registry_recommendations, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "registry_recommendations", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `registry_recommendations` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for registry_recommendations")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == registryRecommendationMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(registryRecommendationType, registryRecommendationMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for registry_recommendations")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.retQuery)
		fmt.Fprintln(boil.DebugWriter, nzUniqueCols...)
	}

	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for registry_recommendations")
	}

CacheNoHooks:
	if !cached {
		registryRecommendationUpsertCacheMut.Lock()
		registryRecommendationUpsertCache[key] = cache
		registryRecommendationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RegistryRecommendation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RegistryRecommendation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryRecommendation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), registryRecommendationPrimaryKeyMapping)
	sql := "DELETE FROM `registry_recommendations` WHERE `id`=?"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from registry_recommendations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for registry_recommendations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q registryRecommendationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no registryRecommendationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registry_recommendations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_recommendations")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RegistryRecommendationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RegistryRecommendation slice provided for delete all")
	}

	if len(o) == 0 {
		return 0, nil
	}

	if len(registryRecommendationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryRecommendationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `registry_recommendations` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryRecommendationPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from registryRecommendation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for registry_recommendations")
	}

	if len(registryRecommendationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RegistryRecommendation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRegistryRecommendation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RegistryRecommendationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RegistryRecommendationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), registryRecommendationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `registry_recommendations`.* FROM `registry_recommendations` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, registryRecommendationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RegistryRecommendationSlice")
	}

	*o = slice

	return nil
}

// RegistryRecommendationExists checks if the RegistryRecommendation row exists.
func RegistryRecommendationExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `registry_recommendations` where `id`=? limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if registry_recommendations exists")
	}

	return exists, nil
}
